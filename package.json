{
  "name": "scoped-var",
  "version": "1.2.0",
  "description": "This is a sass library that enables to limit the scope of custom properties(which is called `CSS Variable`) to `:host`.",
  "scripts": {
    "test": "jest",
    "test:watch": "npm run test -- --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com:nontangent/scoped-var.git"
  },
  "author": "Nozomu Miyamoto(@nontangent)",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "glob": "^7.2.0",
    "jest": "^27.4.5",
    "sass": "^1.45.0",
    "sass-true": "^6.0.1",
    "ts-jest": "^27.1.2"
  }
}
